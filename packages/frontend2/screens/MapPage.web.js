import React from "react";
import { View, Text } from "react-native";

export default function MapPage() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Kaart is niet beschikbaar op web.</Text>
    </View>
  );
}
