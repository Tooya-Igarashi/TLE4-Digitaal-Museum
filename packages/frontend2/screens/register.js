import {StyleSheet, Text, View} from "react-native";

export default function Register() {
    return (
        <View>
            <Text>Registeren</Text>
        </View>
    );

    const styles = StyleSheet.create({})
}